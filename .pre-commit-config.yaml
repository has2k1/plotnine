repos:
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.1.8
    hooks:
      # Run the linter.
      - id: ruff
        args: [--select=I001, --fix]
      # Run the formatter.
      - id: ruff-format
